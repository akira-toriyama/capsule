# capsule profile: cast-rec — placeholder for the "facet-test-rec"
# lineage (the name implies recognition/recording). One of the hand-made
# VMs this repo exists to capture, kept as a slot until its purpose is
# reconstructed. It names no driver, so `make verify PROFILE=cast-rec`
# refuses up front rather than pretending to run.
#
# STATUS: WIP skeleton — purpose to be reconstructed.

name = "cast-rec"

# draft until confirmed (facet? wand cast recognition?)
repos = []

worktree = ""
branch = "main"
patched-deps = []

signing = ""
build = ""
app = ""

fixture = ""
fixture-dest = ""
launch-env = []

# recognition checks lean on AX / headless streams, not pixels
verify-tier = "ax"

driver = ""
